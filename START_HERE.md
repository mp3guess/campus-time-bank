# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ ПО ЗАПУСКУ

## Шаг 1: Откройте терминал и перейдите в папку проекта

### Windows (PowerShell или Command Prompt):
```batch
cd C:\Users\geldi\Desktop\time_bank
```

### Windows (Git Bash):
```bash
cd /c/Users/geldi/Desktop/time_bank
```

### Linux/Mac:
```bash
cd ~/Desktop/time_bank
```
*(или укажите правильный путь к вашему проекту)*

---

## Шаг 2: Запустите бэкенд (Терминал 1)

### Windows:
```batch
cd C:\Users\geldi\Desktop\time_bank
start-app.bat
```

### Linux/Mac:
```bash
cd ~/Desktop/time_bank
./start-app.sh
```

**Что происходит:**
- Скрипт автоматически определяет, есть ли Docker
- Если Docker есть - запускает через Docker Compose
- Если Docker нет - пытается запустить локально с Gradle
- Автоматически запускает PostgreSQL (если нужно)
- Собирает и запускает приложение
- Приложение будет доступно на http://localhost:8080

**Дождитесь сообщения:** `✅ Application is running!` или `Application is starting...`

**Оставьте этот терминал открытым!**

---

## Шаг 3: Откройте НОВЫЙ терминал и запустите фронтенд (Терминал 2)

### Windows:
```batch
cd C:\Users\geldi\Desktop\time_bank
start-frontend.bat
```

### Linux/Mac:
```bash
cd ~/Desktop/time_bank
./start-frontend.sh
```

**Что происходит:**
- Запускается HTTP сервер на порту 8000
- Фронтенд будет доступен на http://localhost:8000/index.html

**Оставьте этот терминал открытым!**

---

## Шаг 4: Откройте браузер

Откройте в браузере:
```
http://localhost:8000/index.html
```

---

## ✅ Проверка работы

### 1. Проверьте Health Check:
```
http://localhost:8080/actuator/health
```
Должно показать: `{"status":"UP"}`

### 2. Проверьте API:
```
http://localhost:8080/api
```

### 3. Проверьте Frontend:
```
http://localhost:8000/index.html
```

---

## 🛑 Остановка

### Остановить бэкенд:
В терминале 1 нажмите `Ctrl+C`

Или если используется Docker:
```batch
docker-compose down
```

### Остановить фронтенд:
В терминале 2 нажмите `Ctrl+C`

---

## 📝 Полный пример (скопируйте и выполните)

### Windows (PowerShell):
```powershell
# Терминал 1: Бэкенд
cd C:\Users\geldi\Desktop\time_bank
.\start-app.bat

# Терминал 2: Фронтенд (в НОВОМ окне терминала)
cd C:\Users\geldi\Desktop\time_bank
.\start-frontend.bat
```

### Windows (Command Prompt):
```batch
# Терминал 1: Бэкенд
cd C:\Users\geldi\Desktop\time_bank
start-app.bat

# Терминал 2: Фронтенд (в НОВОМ окне терминала)
cd C:\Users\geldi\Desktop\time_bank
start-frontend.bat
```

### Linux/Mac:
```bash
# Терминал 1: Бэкенд
cd ~/Desktop/time_bank
./start-app.sh

# Терминал 2: Фронтенд (в НОВОМ окне терминала)
cd ~/Desktop/time_bank
./start-frontend.sh
```

---

## 🔧 Если что-то не работает

### Проблема: "Файл не найден"
**Решение:** Убедитесь, что вы в правильной папке:
```batch
# Проверьте, что вы в правильной папке
dir
# Должны увидеть файлы: start-app.bat, start-frontend.bat, index.html и т.д.
```

### Проблема: "Docker is not running"
**Решение:** Запустите Docker Desktop

### Проблема: "Java is not installed"
**Решение:** Установите Java 17+ с https://adoptium.net/

### Проблема: "Port 8080 is already in use"
**Решение:** Остановите другое приложение на порту 8080

### Проблема: "Port 8000 is already in use"
**Решение:** Остановите другой HTTP сервер или измените порт в скрипте

---

## 📖 Дополнительная информация

- **Подробная инструкция**: `QUICKSTART.md`
- **Решение проблем**: `FIX_RUN_ISSUE.md`
- **Локальный запуск**: `RUN_LOCALLY.md`

---

## 🎯 Готово!

Теперь у вас запущены:
- ✅ Бэкенд на http://localhost:8080
- ✅ Фронтенд на http://localhost:8000/index.html

Можете начинать тестировать приложение! 🚀

