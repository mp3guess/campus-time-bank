# 🚀 Quick Start Guide - Campus TimeBank

## Самый простой способ запуска

### Windows:
```batch
# Запустить всё (бэкенд + база данных)
start-app.bat

# В другом терминале - запустить фронтенд
start-frontend.bat
```

### Linux/Mac:
```bash
# Запустить всё (бэкенд + база данных)
./start-app.sh

# В другом терминале - запустить фронтенд
./start-frontend.sh
```

## Что делает скрипт `start-app.bat` / `start-app.sh`?

1. ✅ Проверяет наличие Docker
2. ✅ Если Docker есть - запускает всё через Docker Compose
3. ✅ Если Docker нет - пытается запустить локально с Gradle Wrapper
4. ✅ Автоматически запускает PostgreSQL (если нужно)
5. ✅ Собирает и запускает приложение
6. ✅ Проверяет, что всё работает

## Что делает скрипт `start-frontend.bat` / `start-frontend.sh`?

1. ✅ Запускает HTTP сервер на порту 8000
2. ✅ Фронтенд доступен на http://localhost:8000/index.html

## Проверка работы

После запуска проверьте:
- ✅ Health: http://localhost:8080/actuator/health
- ✅ API: http://localhost:8080/api
- ✅ Frontend: http://localhost:8000/index.html

## Требования

### Минимальные требования:
- Java 17+ (для локального запуска)
- Docker (рекомендуется, но не обязательно)

### Для фронтенда:
- Python 3 (для HTTP сервера)

## Альтернативные способы запуска

### Вариант 1: Только Docker
```bash
# Запустить бэкенд
./start.sh

# Запустить фронтенд (в другом терминале)
./start-frontend.sh
```

### Вариант 2: Локально с Gradle
```bash
# Убедитесь, что PostgreSQL запущен
# Затем запустите:
./gradlew bootRun

# Или на Windows:
gradlew.bat bootRun
```

### Вариант 3: Через IDE
1. Откройте проект в IntelliJ IDEA / VS Code
2. Дождитесь индексации
3. Запустите `CampusTimeBankApplication.main()`

## Остановка

### Остановить Docker контейнеры:
```bash
docker-compose down
```

### Остановить локальное приложение:
Нажмите `Ctrl+C` в терминале

### Остановить фронтенд:
Нажмите `Ctrl+C` в терминале

## Решение проблем

### Проблема: "Docker is not running"
**Решение:** Запустите Docker Desktop

### Проблема: "Java is not installed"
**Решение:** Установите Java 17+ с https://adoptium.net/

### Проблема: "PostgreSQL connection failed"
**Решение:** Убедитесь, что PostgreSQL запущен на localhost:5432

### Проблема: "Port 8080 is already in use"
**Решение:** Остановите другое приложение на порту 8080 или измените порт в `application.yml`

## Полная инструкция

См. `FIX_RUN_ISSUE.md` для детальной инструкции по решению проблем.

